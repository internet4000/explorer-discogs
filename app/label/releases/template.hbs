{{!-- <h2>Releases</h2> --}}

{{#each-list model as |item|}}
  {{#link-to "release" item.id}}
    {{item.artist}} - {{item.title}}
  {{/link-to}}
  {{#if item.videos}}
    {{play-release release=item}}
  {{/if}}
{{/each-list}}

{{nav-pagination
    path='label.releases'
    currentPage=page
    totalPages=totalPages}}

{{outlet}}

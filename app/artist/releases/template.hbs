{{#each-list list=model.releases as |item|}}

  {{link-to item.title "release" item.id}}

  {{#if item.videos}}
    {{play-release release=item}}
  {{/if}}

  {{#if item.labels}}
    {{#each-list list=item.labels as |label|}}
      <em>{{item.year}}</em> — {{label.name}}
    {{/each-list}}
  {{/if}}

{{/each-list}}

{{outlet}}